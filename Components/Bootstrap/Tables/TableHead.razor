@namespace Uwn.Razor.Components.Bootstrap.Tables
@inherits UwnComponentBase

@if (IsVisible)
{
	<thead id="@Id" class="@ClassNames @AdditionalClassNames">
		@ChildContent
	</thead>
}

@code {
	[Parameter] public RenderFragment? ChildContent { get; set; }
	[CascadingParameter] public TablePurpose Purpose { get; set; }

	protected string ClassNames
	{
		get
		{
			if (IsBootstrapAvailable)
				return Purpose switch
				{
					TablePurpose.Editor => "table-dark",
					_ => string.Empty,
				};
			else
				return string.Empty;
		}
	}
}
